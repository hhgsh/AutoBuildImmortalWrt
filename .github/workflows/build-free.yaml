name: build-free

on:
  workflow_dispatch:
    inputs:
      luci_version:
        description: "选择luci版本"
        required: false
        default: "25.12.2"
        type: choice
        options:
          - "25.12.2"
          - "25.12.1"
      custom_router_ip:
        description: "路由器管理地址"
        required: true
        default: "192.168.0.31"
      profile:
        description: "固件大小 (MB)"
        required: true
        default: "2048"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set executable permissions
        run: |
          [ -f "${{ github.workspace }}/x86-64/build25.sh" ] && chmod +x ${{ github.workspace }}/x86-64/build25.sh || true
          [ -f "${{ github.workspace }}/shell/apk-custom-packages.sh" ] && chmod +x ${{ github.workspace }}/shell/apk-custom-packages.sh || true

      - name: Save Custom Router IP
        run: |
          mkdir -p "${{ github.workspace }}/custom"
          echo "${{ github.event.inputs.custom_router_ip }}" > "${{ github.workspace }}/custom/custom_router_ip.txt"

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses5-dev libncursesw5-dev \
            zlib1g-dev gawk git gettext libssl-dev xsltproc rsync u-boot-tools zstd wget

      - name: Download and Extract ImmortalWrt ImageBuilder
        run: |
          luci_version="${{ github.event.inputs.luci_version }}"
          IB_URL="https://downloads.immortalwrt.org/releases/${luci_version}/targets/x86/64/immortalwrt-imagebuilder-${luci_version}-x86-64.Linux-x86_64.tar.zst"
          
          echo "正在下载 ImageBuilder: ${IB_URL}"
          wget -q --show-progress "${IB_URL}" -O imagebuilder.tar.zst
          
          mkdir -p /tmp/immortalwrt
          tar -I zstd -xf imagebuilder.tar.zst -C /tmp/immortalwrt --strip-components=1
          echo "IMAGEBUILDER_DIR=/tmp/immortalwrt" >> $GITHUB_ENV

      - name: Download APK Packages, Build Index and Export Variables
        run: |
          cd $IMAGEBUILDER_DIR
          mkdir -p packages
          
          echo "========================================="
          echo "正在把第三方离线包精准下载到 ImageBuilder 的 packages 目录..."
          echo "========================================="
          
          # 1. 下载 quickfile
          wget -q -P packages/ https://github.com/sbwml/luci-app-quickfile/releases/download/v1.0.8/luci-app-quickfile_1.0.8-r1_all.apk || true
          wget -q -P packages/ https://github.com/sbwml/luci-app-quickfile/releases/download/v1.0.8/luci-i18n-quickfile-zh-cn_1.0.8-r1_all.apk || true
          
          # 2. 下载 bandix
          wget -q -P packages/ https://github.com/timsaya/openwrt-bandix/releases/download/v0.12.10/bandix-0.12.10-r1_x86_64.apk || true
          wget -q -P packages/ https://github.com/timsaya/luci-app-bandix/releases/download/v0.12.11/luci-app-bandix_0.12.11-r1_all.apk || true
          wget -q -P packages/ https://github.com/timsaya/luci-app-bandix/releases/download/v0.12.11/luci-i18n-bandix-zh-cn_0.12.11-r1_all.apk || true
          
          # 3. 核心步骤：用 apk 生成索引包 APKINDEX.tar.gz
          if command -v apk >/dev/null 2>&1; then
            apk index --allow-untrusted -o packages/APKINDEX.tar.gz packages/*.apk 2>/dev/null || true
          fi
          
          # 将本地 packages 路径加到 repositories.conf 最前排
          echo "$IMAGEBUILDER_DIR/packages" >> repositories.conf
          
          # 导入 Shell 配置中的包名变量
          APK_SCRIPT="${{ github.workspace }}/shell/apk-custom-packages.sh"
          if [ -f "$APK_SCRIPT" ]; then
            source "$APK_SCRIPT"
            echo "CUSTOM_PACKAGES=$CUSTOM_PACKAGES" >> $GITHUB_ENV
          fi
          
          echo "=== 已下载文件与生成的 APKINDEX 索引 ==="
          ls -la packages/

      - name: Build Firmware with build25.sh
        run: |
          cd $IMAGEBUILDER_DIR
          
          mkdir -p bin files files/etc/config
          [ -d "${{ github.workspace }}/files" ] && cp -rf ${{ github.workspace }}/files/* files/ 2>/dev/null || true
          [ -d "${{ github.workspace }}/custom" ] && cp -rf ${{ github.workspace }}/custom/* files/etc/config/ 2>/dev/null || true
          [ -f "${{ github.workspace }}/x86-64/imm25.config" ] && cp -f ${{ github.workspace }}/x86-64/imm25.config .config
          
          export PROFILE="generic"
          export CUSTOM_ROUTER_IP="${{ github.event.inputs.custom_router_ip }}"
          export CUSTOM_PACKAGES="${CUSTOM_PACKAGES}"
          
          BUILD_SCRIPT="${{ github.workspace }}/x86-64/build25.sh"
          if [ -f "$BUILD_SCRIPT" ]; then
            echo "运行 x86-64/build25.sh..."
            bash "$BUILD_SCRIPT"
          else
            make image PROFILE="generic" PACKAGES="$CUSTOM_PACKAGES" FILES="files"
          fi
          
          # 提取消并保存所有生成的镜像包
          mkdir -p ${{ github.workspace }}/release_bin
          find bin/targets/x86/64/ -type f \( -name "*.img.gz" -o -name "*.iso" \) -exec cp -f {} ${{ github.workspace }}/release_bin/ \;
          
          echo "=== 成功提取到的发布固件文件 ==="
          ls -la ${{ github.workspace }}/release_bin/

      - name: Create Release Info
        run: |
          cat > ${{ github.workspace }}/router-info.md <<EOF
          ## 📡 ImmortalWrt ${{ github.event.inputs.luci_version }} 固件信息
          - 🏠 **后台地址**: ${{ github.event.inputs.custom_router_ip }}
          - 👤 **用户名**: root
          - 🔑 **密码**: 无
          EOF

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: Laohu-x86-64-${{ github.event.inputs.luci_version }}
          body_path: ${{ github.workspace }}/router-info.md
          files: |
            ${{ github.workspace }}/release_bin/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
